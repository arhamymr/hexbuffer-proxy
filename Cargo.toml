[package]
name = "hexbuffer-proxy"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1.0", features = ["full"] }
tokio-rustls = "0.26" # Raw async TLS streams
rustls-pki-types = "1.0"
rcgen = "0.13" # Generates dummy site certs on the fly
anyhow = "1.0"